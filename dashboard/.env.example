# Xcode xip artifact (preferred install path — no Apple auth on runners)
S3_XIP_URI=s3://your-bucket/Xcode_26.0.xip
AWS_REGION=us-east-1

# Optional fallback if a job must use xcodes instead of xip:
# XCODE_APPLE_ID=
# XCODE_APPLE_ID_PASSWORD=

# PORT=4400
# HOSTS_PATH=hosts.yaml
# DATA_DIR=data
# PROBE_INTERVAL_MS=300000
